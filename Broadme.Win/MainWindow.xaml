<Window x:Class="Broadme.Win.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Broadme.Win.ViewModels"
        mc:Ignorable="d"
        Title="Broadme"
        Width="938"
        Height="269"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="White"
        WindowStyle="SingleBorderWindow">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Window.Resources>
        <LinearGradientBrush x:Key="StartBrush" StartPoint="0,0" EndPoint="0.7,1">
            <GradientStop Color="#8E5BDA" Offset="0.02" />
            <GradientStop Color="#AD6BE0" Offset="0.65" />
            <GradientStop Color="#CB7AE6" Offset="0.98" />
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="StopBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#E76DB8" Offset="0" />
            <GradientStop Color="#DF4C9D" Offset="1" />
        </LinearGradientBrush>

        <Style x:Key="OutlineBtn" TargetType="Button">
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="#8439B0" />
            <Setter Property="BorderBrush" Value="#BFA6D5" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="16,4" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="33,20,33,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="False">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                <Image Source="/Assets/app-logo.png" Width="40" Height="40" Margin="0,0,12,0"/>
                <TextBlock Text="Broadme" FontSize="32" FontWeight="Medium" Foreground="#8439B0" VerticalAlignment="Center" />
                <Border Background="#8439B0" CornerRadius="10" Margin="8,0,0,0" Padding="8,3">
                    <TextBlock Text="Beta" Foreground="White" FontSize="12" FontWeight="SemiBold" />
                </Border>
            </StackPanel>
        </DockPanel>

        <Grid Grid.Row="1" Margin="0,12,0,8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,0,0,12">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <TextBlock Text="Clients: " FontSize="24" Foreground="#9F8D9E" />
                    <TextBlock Text="{Binding ClientCount}" FontSize="24" Foreground="#9F8D9E" />
                </StackPanel>

                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="IP: " FontSize="15" Foreground="#B8A4B3" />
                    <TextBlock Text="{Binding CurrentIp}" FontSize="15" Foreground="#B8A4B3" />
                    <TextBlock Text="  |  URL: " FontSize="15" Foreground="#B8A4B3" />
                    <TextBlock Text="{Binding StreamUrl}" FontSize="15" Foreground="#B8A4B3" />
                </StackPanel>
            </DockPanel>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="126"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="126"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="135"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="125"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Height="31" Command="{Binding ToggleBroadcastCommand}" Foreground="White" BorderThickness="0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="{StaticResource StartBrush}"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border CornerRadius="20" Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsStreaming}" Value="True">
                                    <Setter Property="Background" Value="{StaticResource StopBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <TextBlock Text="{Binding StartStopText}" Foreground="White" FontSize="16" FontWeight="Medium"/>
                </Button>

                <Button Grid.Column="2" Height="31" Command="{Binding CopyUrlCommand}" Style="{StaticResource OutlineBtn}" Content="複製URL" />
                <Button Grid.Column="4" Height="31" Style="{StaticResource OutlineBtn}" Content="顯示QRCode" Command="{Binding ShowQrCommand}"/>
                <Button Grid.Column="6" Height="31" Command="{Binding OpenControlAuthCommand}" Style="{StaticResource OutlineBtn}" Content="控制授權" />

                <ComboBox Grid.Column="8" Height="31"
                          ItemsSource="{Binding ResolutionOptions}"
                          SelectedItem="{Binding SelectedResolution}"
                          IsEnabled="{Binding IsNotStreaming}"/>

                <ComboBox Grid.Column="10" Height="31"
                          ItemsSource="{Binding FpsOptions}"
                          SelectedItem="{Binding SelectedFps}"
                          IsEnabled="{Binding IsNotStreaming}"/>

                <ComboBox Grid.Column="12" Height="31"
                          ItemsSource="{Binding NetworkInterfaces}"
                          SelectedItem="{Binding SelectedInterface}"
                          DisplayMemberPath="Label"
                          IsEnabled="{Binding IsNotStreaming}"/>

                <Button Grid.Column="14" Height="31" Style="{StaticResource OutlineBtn}" Content="效能日誌" Command="{Binding OpenLogCommand}"/>
            </Grid>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Vertical" HorizontalAlignment="Center">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#B8A4B3" FontSize="12" HorizontalAlignment="Center" Margin="0,0,0,4"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="©廣臻科技版權所有 | " Foreground="#B8A4B3" FontSize="15" />
                <TextBlock Text="官方LINE" Foreground="#8439B0" FontSize="15" TextDecorations="Underline" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
