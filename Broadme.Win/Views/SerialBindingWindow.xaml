<Window x:Class="Broadme.Win.Views.SerialBindingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Serial Binding" Width="938" Height="269" ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        Background="Transparent" WindowStyle="None" AllowsTransparency="True">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="44" CornerRadius="20" GlassFrameThickness="0" ResizeBorderThickness="0" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Border CornerRadius="20" Background="White" BorderBrush="#EEE" BorderThickness="1" Padding="30">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0">
                <TextBlock Text="Broadme" FontSize="32" Foreground="#8439B0" />
                <Button DockPanel.Dock="Right" Width="28" Height="28" Click="CloseClick" Background="Transparent" BorderThickness="0">
                    <Image Source="/Assets/close-btn.png" Stretch="Uniform"/>
                </Button>
            </DockPanel>

            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Border Background="#F6F2F7" CornerRadius="20" Padding="18" Width="560">
                    <DockPanel>
                        <TextBlock Text="序號註冊:" FontSize="20" Foreground="#846993" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox x:Name="SerialTextBox" Width="300" Height="31" VerticalContentAlignment="Center"/>
                        <Button x:Name="BindButton" Content="綁定" Width="70" Height="31" Margin="10,0,0,0" Click="BindClick"/>
                    </DockPanel>
                </Border>
                <TextBlock x:Name="MessageText" Margin="0,10,0,0" Foreground="Red"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
