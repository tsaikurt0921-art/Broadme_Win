<Window x:Class="Broadme.Win.Views.QrCodeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="QRCode" Width="480" Height="560" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" Background="White">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" CornerRadius="20" GlassFrameThickness="0" ResizeBorderThickness="0" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="16,16,16,8">
            <Button Width="44" Height="44" HorizontalAlignment="Left" VerticalAlignment="Top" Click="CloseClick" BorderBrush="#BFA6D5" BorderThickness="1" Background="White">
                <Viewbox Width="18" Height="18">
                    <Path Stroke="#7C43B3" StrokeThickness="3.2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Data="M14,2 L3,13 L14,24"/>
                </Viewbox>
            </Button>
        </Grid>

        <Border Grid.Row="1" Margin="24,0,24,0" BorderBrush="#EFEFEF" BorderThickness="1" CornerRadius="12" Padding="16">
            <Image x:Name="QrImage" Stretch="Uniform"/>
        </Border>

        <Button Grid.Row="2" Margin="24,14,24,22" Height="56" Click="CopyClick" Background="#E9DDFA" BorderBrush="#E9DDFA" BorderThickness="2">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="📋 " FontSize="20" Foreground="#8439B0"/>
                <TextBlock x:Name="UrlText" FontSize="16" Foreground="#8439B0"/>
            </StackPanel>
        </Button>
    </Grid>
</Window>
