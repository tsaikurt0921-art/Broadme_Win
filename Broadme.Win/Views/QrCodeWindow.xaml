<Window x:Class="Broadme.Win.Views.QrCodeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="QRCode" Width="938" Height="805" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" Background="White">
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="24,24,24,10">
            <Button Width="64" Height="64" HorizontalAlignment="Left" VerticalAlignment="Top" Click="CloseClick" BorderBrush="#BFA6D5" BorderThickness="1" Background="White">
                <Viewbox Width="22" Height="22">
                    <Path Stroke="#7C43B3" StrokeThickness="3.2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Data="M14,2 L3,13 L14,24"/>
                </Viewbox>
            </Button>
        </Grid>

        <Border Grid.Row="1" Margin="90,0,90,0" BorderBrush="#EFEFEF" BorderThickness="1" CornerRadius="12" Padding="20">
            <Image x:Name="QrImage" Stretch="Uniform"/>
        </Border>

        <Button Grid.Row="2" Margin="263,18,263,40" Height="70" Click="CopyClick" Background="#E9DDFA" BorderBrush="#E9DDFA" BorderThickness="2">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="📋 " FontSize="20" Foreground="#8439B0"/>
                <TextBlock x:Name="UrlText" FontSize="16" Foreground="#8439B0"/>
            </StackPanel>
        </Button>
    </Grid>
</Window>
